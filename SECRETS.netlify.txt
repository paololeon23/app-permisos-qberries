# Seguridad Netlify — NO pegues aquí las URLs reales ni el token
# (Netlify falla el build si encuentra secretos en el código)
#
# En Netlify → Environment variables (ya las tienes):
#   API_TOKEN
#   PERMISOS_SCRIPT_URL
#   TRABAJADORES_SCRIPT_URL
#
# En Apps Script → Propiedades del script:
#   API_TOKEN = (el mismo valor que en Netlify)
#
# App pública solo usa:
#   /api/permisos
#   /api/trabajadores
