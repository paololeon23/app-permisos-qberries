/**
 * OBSOLETO — este archivo unificado ya no se usa.
 *
 * Usa los 2 scripts separados (2 Spreadsheets distintos):
 *   apps-script/01-permisos/code.gs        → PERMISOS_URL
 *   apps-script/02-trabajadores/code.gs    → TRABAJADORES_URL
 *
 * Luego pega cada URL en core/api-config.js
 */
